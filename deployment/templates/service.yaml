apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  namespace: mdai
  labels:
    app: mdai-collector-logs
spec:
  selector:
    app: mdai-collector-logs
  ports:
    - port: 4400
      targetPort: http
      protocol: TCP
      name: http
  type: ClusterIP